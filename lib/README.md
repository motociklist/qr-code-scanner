# Структура проекта QR Code Scanner

## 📁 Структура папок

### `screens/`
Экраны приложения (страницы):
- `splash_screen.dart` - Экран загрузки
- `onboarding_screen.dart` - Онбординг (4 страницы)
- `home_screen.dart` - Главный экран с навигацией
- `qr_scanner_screen.dart` - Экран сканирования QR-кодов
- `result_screen.dart` - Экран результата сканирования
- `history_screen.dart` - История сканирований
- `my_qr_codes_screen.dart` - Мои QR-коды
- `create_qr_screen.dart` - Создание QR-кода
- `pricing_screen.dart` - Экран подписки/прайс-лист

### `models/`
Модели данных:
- `scan_history_item.dart` - Элемент истории сканирования
- `saved_qr_code.dart` - Сохраненный QR-код

### `services/`
Сервисы для работы с данными:
- `history_service.dart` - Управление историей сканирований
- `saved_qr_service.dart` - Управление сохраненными QR-кодами

### `components/`
Переиспользуемые UI компоненты:
- `filter_chips.dart` - Горизонтальные фильтры/чипы
- `action_buttons.dart` - Кнопки действий (Copy, Share)
- `icon_circle.dart` - Круглая иконка с фоном
- `empty_state.dart` - Пустое состояние экрана

### `widgets/`
Переиспользуемые виджеты:
- `screen_header.dart` - Заголовок экрана с кнопкой закрытия
- `feature_card.dart` - Карточка функции/фичи

### `utils/`
Утилиты и вспомогательные функции:
- `date_formatter.dart` - Форматирование дат и времени
- `url_helper.dart` - Работа с URL и текстом
- `qr_type_helper.dart` - Определение типа QR-кода и иконок

### `constants/`
Константы приложения:
- `app_colors.dart` - Цветовая палитра приложения
- `app_styles.dart` - Текстовые стили и декорации

## 🎯 Принципы организации

1. **Разделение ответственности**: Каждый файл отвечает за одну задачу
2. **Переиспользование**: Общие компоненты вынесены в `components/` и `widgets/`
3. **Утилиты**: Вспомогательные функции в `utils/`
4. **Константы**: Все константы (цвета, стили) в `constants/`
5. **Модели**: Структуры данных в `models/`
6. **Сервисы**: Бизнес-логика в `services/`

## 📦 Зависимости

Основные пакеты:
- `mobile_scanner` - Сканирование QR-кодов
- `permission_handler` - Управление разрешениями
- `shared_preferences` - Локальное хранилище
- `url_launcher` - Открытие URL
- `share_plus` - Поделиться контентом
- `image_picker` - Выбор изображений из галереи
- `flutter_svg` - Отображение SVG
- `intl` - Форматирование дат

